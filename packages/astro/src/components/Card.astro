---
import type { HTMLAttributes } from "astro/types";

type Props = {
  class?: string;
  as?: string;
} & HTMLAttributes<"div">;

const { class: className, as: Tag = "div", ...rest } = Astro.props as Props;
---

<Tag class:list={["card", className]} {...rest}>
  <slot />
</Tag>
